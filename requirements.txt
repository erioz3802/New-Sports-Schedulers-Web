# requirements.txt - Sports Scheduler Web Application (FIXED - Enhanced Bulk Operations)
# Core Flask dependencies
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Werkzeug==2.3.7

# Excel processing for bulk operations
openpyxl==3.1.2
xlrd==2.0.1

# Compatible numpy and pandas versions for data processing (optional - only if needed)
# numpy==1.24.3
# pandas==2.0.3

# Additional dependencies for enhanced functionality
requests==2.31.0        # For API calls (Google Maps, etc.)
python-dateutil==2.8.2  # Better date handling

# Email functionality (for future notifications)
Flask-Mail==0.9.1

# Development and debugging
python-dotenv==1.0.0    # Environment variable management

# NOTE: secure_filename is included in Werkzeug - no separate package needed
# from werkzeug.utils import secure_filename